default_platform(:ios)

platform :ios do
  desc "Build GlucoDash en upload naar TestFlight"
  lane :build_upload do

    # API key voor App Store Connect
    api_key = app_store_connect_api_key(
      key_id: ENV['FASTLANE_KEY_ID'],
      issuer_id: ENV['FASTLANE_ISSUER_ID'],
      key_filepath: File.expand_path("../api_key.p8", __dir__)
    )

    keychain_name = "build.keychain"
    keychain_path = File.expand_path("~/Library/Keychains/build.keychain-db")
    keychain_pass = "buildpass"

    # Maak tijdelijke keychain aan voor CI
    create_keychain(
      name: keychain_name,
      password: keychain_pass,
      default_keychain: true,
      unlock: true,
      timeout: 3600,
      lock_when_sleeps: false
    )

    # Match: haalt cert + provisioning profile op uit versleutelde git repo
    # Eerste run: maakt cert aan en slaat alles op
    # Volgende runs: hergebruikt opgeslagen cert (geen nieuw cert nodig)
    match(
      type: "appstore",
      api_key: api_key,
      app_identifier: "com.#{ENV['TEAMID']}.glucodash",
      keychain_name: keychain_name,
      keychain_password: keychain_pass,
      readonly: false,
      force: false
    )

    # Zorg dat codesign de private key kan gebruiken zonder UI-prompt
    sh("security set-key-partition-list -S apple-tool:,apple: -s -k '#{keychain_pass}' '#{keychain_path}' 2>/dev/null || true")

    update_app_identifier(
      xcodeproj: "ios/App/App.xcodeproj",
      plist_path: "App/Info.plist",
      app_identifier: "com.#{ENV['TEAMID']}.glucodash"
    )

    # Match stelt de profile_name in via lane_context
    profile_name = ENV["sigh_com.#{ENV['TEAMID']}.glucodash_appstore_profile-name"] || "match AppStore com.#{ENV['TEAMID']}.glucodash"

    update_code_signing_settings(
      use_automatic_signing: false,
      team_id: ENV["TEAMID"],
      path: "ios/App/App.xcodeproj",
      profile_name: profile_name,
      code_sign_identity: "iPhone Distribution"
    )

    # Gebruik GitHub run number als build number — altijd uniek en stijgend
    build_number = ENV['GITHUB_RUN_NUMBER'] || Time.now.to_i.to_s
    increment_build_number(
      build_number: build_number,
      xcodeproj: "ios/App/App.xcodeproj"
    )

    build_app(
      workspace: "ios/App/App.xcworkspace",
      scheme: "App",
      configuration: "Release",
      export_method: "app-store",
      export_options: {
        provisioningProfiles: {
          "com.#{ENV['TEAMID']}.glucodash" => profile_name
        }
      }
    )

    upload_to_testflight(
      api_key: api_key,
      skip_waiting_for_build_processing: true
    )
  end
end
